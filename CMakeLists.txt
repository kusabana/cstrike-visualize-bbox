# This file is automatically generated from cmake.toml - DO NOT EDIT
# See https://github.com/build-cpp/cmkr for more information

cmake_minimum_required(VERSION 3.15)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_BINARY_DIR)
	message(FATAL_ERROR "In-tree builds are not supported. Run CMake from a separate directory: cmake -B build")
endif()

set(CMKR_ROOT_PROJECT OFF)
if(CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)
	set(CMKR_ROOT_PROJECT ON)

	# Bootstrap cmkr and automatically regenerate CMakeLists.txt
	include(cmkr.cmake OPTIONAL RESULT_VARIABLE CMKR_INCLUDE_RESULT)
	if(CMKR_INCLUDE_RESULT)
		cmkr()
	endif()

	# Enable folder support
	set_property(GLOBAL PROPERTY USE_FOLDERS ON)

	# Create a configure-time dependency on cmake.toml to improve IDE support
	configure_file(cmake.toml cmake.toml COPYONLY)
endif()

project(cstrike-visualize-bbox
	LANGUAGES
		CXX
	VERSION
		0.1.0
)

include(FetchContent)

message(STATUS "Fetching eleos (v1.0.0)...")
FetchContent_Declare(eleos
	GIT_REPOSITORY
		"https://github.com/kusabana/eleos"
	GIT_TAG
		v1.0.0
)
FetchContent_MakeAvailable(eleos)

# Target: visualize_bbox
set(visualize_bbox_SOURCES
	"src/plugin.cpp"
	"include/plugin.hpp"
	"include/valve.hpp"
	cmake.toml
)

add_library(visualize_bbox SHARED)

target_sources(visualize_bbox PRIVATE ${visualize_bbox_SOURCES})
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${visualize_bbox_SOURCES})

target_compile_features(visualize_bbox PUBLIC
	cxx_std_20
)

target_compile_options(visualize_bbox PUBLIC
	-m32
)

target_include_directories(visualize_bbox PUBLIC
	include
)

target_link_libraries(visualize_bbox PUBLIC
	eleos
)

target_link_options(visualize_bbox PUBLIC
	-m32
)

